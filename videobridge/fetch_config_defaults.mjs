

// Get the reference configs
// Get the default configs and related files from the docker build repo

// Build the config files with the basic env variables
// Compare with our current config file
// In case of differences, update the config file
